#include <stdio.h>
/**
 * main- prints all single digit base 10 numbers
 *
 * Return: Always returns 0 (Successful)
*/
int main(void)
{
for (int i = 48; i < 58; i++)
putchar(i);

putchar('\n');

return (0);
}
