#include <stdio.h>
/**
 * main- prints single digit base 10 numbers
 *
 * Return: always returns 0 (successful)
 */
int main(void)
{
int i;

for (i = 0; i < 10; i++)
printf("%d", i);

putchar('\n');

return (0);
}
